using System.Collections.Generic;
using Server.Domain;

namespace Server.Infrastructure.Utilities
{
    public static class TemplateUtility
    {
        
        public static Template CreateFromSolution(Solution solution) {

            var template = new Template();
            
            return template;
        }
    }
}